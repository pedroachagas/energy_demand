from datetime import datetime, timedelta
import time
from pandas import Timestamp
from pandera import DataFrameSchema, Column, Check, Index

from src.config.config import config
from src.utils.date_utils import check_time_interval

LOWER_BOUND = 500_000
UPPER_BOUND = 1_100_000

schema = DataFrameSchema(
    columns={
        "ds": Column(
            dtype="datetime64[ns]",
            checks=[
                Check.greater_than_or_equal_to(Timestamp(config.START_DATE)),
                Check.less_than_or_equal_to((datetime.now() + timedelta(days=60))),
                Check(lambda x: check_time_interval(x, timedelta(days=1)), element_wise=False, error="Time interval should be 1 day"),
            ],
            nullable=False,
            unique=False,
            coerce=False,
            required=True,
            regex=False,
            description=None,
            title=None,
        ),
        "y": Column(
            dtype="float64",
            checks=[
                Check.greater_than_or_equal_to(LOWER_BOUND),
                Check.less_than_or_equal_to(UPPER_BOUND),
            ],
            nullable=True,
            unique=False,
            coerce=False,
            required=True,
            regex=False,
            description=None,
            title=None,
        ),
        "unique_id": Column(
            dtype="int64",
            checks=[
                Check.equal_to(0),
            ],
            nullable=False,
            unique=False,
            coerce=False,
            required=True,
            regex=False,
            description=None,
            title=None,
        ),
        "CatBoostRegressor": Column(
            dtype="float64",
            checks=[
                Check.greater_than_or_equal_to(LOWER_BOUND),
                Check.less_than_or_equal_to(UPPER_BOUND),
            ],
            nullable=True,
            unique=False,
            coerce=False,
            required=True,
            regex=False,
            description=None,
            title=None,
        ),
        "LGBMRegressor": Column(
            dtype="float64",
            checks=[
                Check.greater_than_or_equal_to(LOWER_BOUND),
                Check.less_than_or_equal_to(UPPER_BOUND),
            ],
            nullable=True,
            unique=False,
            coerce=False,
            required=True,
            regex=False,
            description=None,
            title=None,
        ),
        "XGBRegressor": Column(
            dtype="float32",
            checks=[
                Check.greater_than_or_equal_to(LOWER_BOUND),
                Check.less_than_or_equal_to(UPPER_BOUND),
            ],
            nullable=True,
            unique=False,
            coerce=False,
            required=True,
            regex=False,
            description=None,
            title=None,
        ),
        "RandomForestRegressor": Column(
            dtype="float64",
            checks=[
                Check.greater_than_or_equal_to(LOWER_BOUND),
                Check.less_than_or_equal_to(UPPER_BOUND),
            ],
            nullable=True,
            unique=False,
            coerce=False,
            required=True,
            regex=False,
            description=None,
            title=None,
        ),
        "CatBoostRegressor-lo-99": Column(
            dtype="float64",
            checks=[
                Check.greater_than_or_equal_to(LOWER_BOUND),
                Check.less_than_or_equal_to(UPPER_BOUND),
            ],
            nullable=True,
            unique=False,
            coerce=False,
            required=True,
            regex=False,
            description=None,
            title=None,
        ),
        "CatBoostRegressor-lo-95": Column(
            dtype="float64",
            checks=[
                Check.greater_than_or_equal_to(LOWER_BOUND),
                Check.less_than_or_equal_to(UPPER_BOUND),
            ],
            nullable=True,
            unique=False,
            coerce=False,
            required=True,
            regex=False,
            description=None,
            title=None,
        ),
        "CatBoostRegressor-lo-90": Column(
            dtype="float64",
            checks=[
                Check.greater_than_or_equal_to(LOWER_BOUND),
                Check.less_than_or_equal_to(UPPER_BOUND),
            ],
            nullable=True,
            unique=False,
            coerce=False,
            required=True,
            regex=False,
            description=None,
            title=None,
        ),
        "CatBoostRegressor-lo-80": Column(
            dtype="float64",
            checks=[
                Check.greater_than_or_equal_to(LOWER_BOUND),
                Check.less_than_or_equal_to(UPPER_BOUND),
            ],
            nullable=True,
            unique=False,
            coerce=False,
            required=True,
            regex=False,
            description=None,
            title=None,
        ),
        "CatBoostRegressor-lo-70": Column(
            dtype="float64",
            checks=[
                Check.greater_than_or_equal_to(LOWER_BOUND),
                Check.less_than_or_equal_to(UPPER_BOUND),
            ],
            nullable=True,
            unique=False,
            coerce=False,
            required=True,
            regex=False,
            description=None,
            title=None,
        ),
        "CatBoostRegressor-lo-60": Column(
            dtype="float64",
            checks=[
                Check.greater_than_or_equal_to(LOWER_BOUND),
                Check.less_than_or_equal_to(UPPER_BOUND),
            ],
            nullable=True,
            unique=False,
            coerce=False,
            required=True,
            regex=False,
            description=None,
            title=None,
        ),
        "CatBoostRegressor-lo-50": Column(
            dtype="float64",
            checks=[
                Check.greater_than_or_equal_to(LOWER_BOUND),
                Check.less_than_or_equal_to(UPPER_BOUND),
            ],
            nullable=True,
            unique=False,
            coerce=False,
            required=True,
            regex=False,
            description=None,
            title=None,
        ),
        "CatBoostRegressor-lo-40": Column(
            dtype="float64",
            checks=[
                Check.greater_than_or_equal_to(LOWER_BOUND),
                Check.less_than_or_equal_to(UPPER_BOUND),
            ],
            nullable=True,
            unique=False,
            coerce=False,
            required=True,
            regex=False,
            description=None,
            title=None,
        ),
        "CatBoostRegressor-lo-30": Column(
            dtype="float64",
            checks=[
                Check.greater_than_or_equal_to(LOWER_BOUND),
                Check.less_than_or_equal_to(UPPER_BOUND),
            ],
            nullable=True,
            unique=False,
            coerce=False,
            required=True,
            regex=False,
            description=None,
            title=None,
        ),
        "CatBoostRegressor-lo-20": Column(
            dtype="float64",
            checks=[
                Check.greater_than_or_equal_to(LOWER_BOUND),
                Check.less_than_or_equal_to(UPPER_BOUND),
            ],
            nullable=True,
            unique=False,
            coerce=False,
            required=True,
            regex=False,
            description=None,
            title=None,
        ),
        "CatBoostRegressor-lo-10": Column(
            dtype="float64",
            checks=[
                Check.greater_than_or_equal_to(LOWER_BOUND),
                Check.less_than_or_equal_to(UPPER_BOUND),
            ],
            nullable=True,
            unique=False,
            coerce=False,
            required=True,
            regex=False,
            description=None,
            title=None,
        ),
        "CatBoostRegressor-hi-10": Column(
            dtype="float64",
            checks=[
                Check.greater_than_or_equal_to(LOWER_BOUND),
                Check.less_than_or_equal_to(UPPER_BOUND),
            ],
            nullable=True,
            unique=False,
            coerce=False,
            required=True,
            regex=False,
            description=None,
            title=None,
        ),
        "CatBoostRegressor-hi-20": Column(
            dtype="float64",
            checks=[
                Check.greater_than_or_equal_to(LOWER_BOUND),
                Check.less_than_or_equal_to(UPPER_BOUND),
            ],
            nullable=True,
            unique=False,
            coerce=False,
            required=True,
            regex=False,
            description=None,
            title=None,
        ),
        "CatBoostRegressor-hi-30": Column(
            dtype="float64",
            checks=[
                Check.greater_than_or_equal_to(LOWER_BOUND),
                Check.less_than_or_equal_to(UPPER_BOUND),
            ],
            nullable=True,
            unique=False,
            coerce=False,
            required=True,
            regex=False,
            description=None,
            title=None,
        ),
        "CatBoostRegressor-hi-40": Column(
            dtype="float64",
            checks=[
                Check.greater_than_or_equal_to(LOWER_BOUND),
                Check.less_than_or_equal_to(UPPER_BOUND),
            ],
            nullable=True,
            unique=False,
            coerce=False,
            required=True,
            regex=False,
            description=None,
            title=None,
        ),
        "CatBoostRegressor-hi-50": Column(
            dtype="float64",
            checks=[
                Check.greater_than_or_equal_to(LOWER_BOUND),
                Check.less_than_or_equal_to(UPPER_BOUND),
            ],
            nullable=True,
            unique=False,
            coerce=False,
            required=True,
            regex=False,
            description=None,
            title=None,
        ),
        "CatBoostRegressor-hi-60": Column(
            dtype="float64",
            checks=[
                Check.greater_than_or_equal_to(LOWER_BOUND),
                Check.less_than_or_equal_to(UPPER_BOUND),
            ],
            nullable=True,
            unique=False,
            coerce=False,
            required=True,
            regex=False,
            description=None,
            title=None,
        ),
        "CatBoostRegressor-hi-70": Column(
            dtype="float64",
            checks=[
                Check.greater_than_or_equal_to(LOWER_BOUND),
                Check.less_than_or_equal_to(UPPER_BOUND),
            ],
            nullable=True,
            unique=False,
            coerce=False,
            required=True,
            regex=False,
            description=None,
            title=None,
        ),
        "CatBoostRegressor-hi-80": Column(
            dtype="float64",
            checks=[
                Check.greater_than_or_equal_to(LOWER_BOUND),
                Check.less_than_or_equal_to(UPPER_BOUND),
            ],
            nullable=True,
            unique=False,
            coerce=False,
            required=True,
            regex=False,
            description=None,
            title=None,
        ),
        "CatBoostRegressor-hi-90": Column(
            dtype="float64",
            checks=[
                Check.greater_than_or_equal_to(LOWER_BOUND),
                Check.less_than_or_equal_to(UPPER_BOUND),
            ],
            nullable=True,
            unique=False,
            coerce=False,
            required=True,
            regex=False,
            description=None,
            title=None,
        ),
        "CatBoostRegressor-hi-95": Column(
            dtype="float64",
            checks=[
                Check.greater_than_or_equal_to(LOWER_BOUND),
                Check.less_than_or_equal_to(UPPER_BOUND),
            ],
            nullable=True,
            unique=False,
            coerce=False,
            required=True,
            regex=False,
            description=None,
            title=None,
        ),
        "CatBoostRegressor-hi-99": Column(
            dtype="float64",
            checks=[
                Check.greater_than_or_equal_to(LOWER_BOUND),
                Check.less_than_or_equal_to(UPPER_BOUND),
            ],
            nullable=True,
            unique=False,
            coerce=False,
            required=True,
            regex=False,
            description=None,
            title=None,
        ),
        "LGBMRegressor-lo-99": Column(
            dtype="float64",
            checks=[
                Check.greater_than_or_equal_to(LOWER_BOUND),
                Check.less_than_or_equal_to(UPPER_BOUND),
            ],
            nullable=True,
            unique=False,
            coerce=False,
            required=True,
            regex=False,
            description=None,
            title=None,
        ),
        "LGBMRegressor-lo-95": Column(
            dtype="float64",
            checks=[
                Check.greater_than_or_equal_to(LOWER_BOUND),
                Check.less_than_or_equal_to(UPPER_BOUND),
            ],
            nullable=True,
            unique=False,
            coerce=False,
            required=True,
            regex=False,
            description=None,
            title=None,
        ),
        "LGBMRegressor-lo-90": Column(
            dtype="float64",
            checks=[
                Check.greater_than_or_equal_to(LOWER_BOUND),
                Check.less_than_or_equal_to(UPPER_BOUND),
            ],
            nullable=True,
            unique=False,
            coerce=False,
            required=True,
            regex=False,
            description=None,
            title=None,
        ),
        "LGBMRegressor-lo-80": Column(
            dtype="float64",
            checks=[
                Check.greater_than_or_equal_to(LOWER_BOUND),
                Check.less_than_or_equal_to(UPPER_BOUND),
            ],
            nullable=True,
            unique=False,
            coerce=False,
            required=True,
            regex=False,
            description=None,
            title=None,
        ),
        "LGBMRegressor-lo-70": Column(
            dtype="float64",
            checks=[
                Check.greater_than_or_equal_to(LOWER_BOUND),
                Check.less_than_or_equal_to(UPPER_BOUND),
            ],
            nullable=True,
            unique=False,
            coerce=False,
            required=True,
            regex=False,
            description=None,
            title=None,
        ),
        "LGBMRegressor-lo-60": Column(
            dtype="float64",
            checks=[
                Check.greater_than_or_equal_to(LOWER_BOUND),
                Check.less_than_or_equal_to(UPPER_BOUND),
            ],
            nullable=True,
            unique=False,
            coerce=False,
            required=True,
            regex=False,
            description=None,
            title=None,
        ),
        "LGBMRegressor-lo-50": Column(
            dtype="float64",
            checks=[
                Check.greater_than_or_equal_to(LOWER_BOUND),
                Check.less_than_or_equal_to(UPPER_BOUND),
            ],
            nullable=True,
            unique=False,
            coerce=False,
            required=True,
            regex=False,
            description=None,
            title=None,
        ),
        "LGBMRegressor-lo-40": Column(
            dtype="float64",
            checks=[
                Check.greater_than_or_equal_to(LOWER_BOUND),
                Check.less_than_or_equal_to(UPPER_BOUND),
            ],
            nullable=True,
            unique=False,
            coerce=False,
            required=True,
            regex=False,
            description=None,
            title=None,
        ),
        "LGBMRegressor-lo-30": Column(
            dtype="float64",
            checks=[
                Check.greater_than_or_equal_to(LOWER_BOUND),
                Check.less_than_or_equal_to(UPPER_BOUND),
            ],
            nullable=True,
            unique=False,
            coerce=False,
            required=True,
            regex=False,
            description=None,
            title=None,
        ),
        "LGBMRegressor-lo-20": Column(
            dtype="float64",
            checks=[
                Check.greater_than_or_equal_to(LOWER_BOUND),
                Check.less_than_or_equal_to(UPPER_BOUND),
            ],
            nullable=True,
            unique=False,
            coerce=False,
            required=True,
            regex=False,
            description=None,
            title=None,
        ),
        "LGBMRegressor-lo-10": Column(
            dtype="float64",
            checks=[
                Check.greater_than_or_equal_to(LOWER_BOUND),
                Check.less_than_or_equal_to(UPPER_BOUND),
            ],
            nullable=True,
            unique=False,
            coerce=False,
            required=True,
            regex=False,
            description=None,
            title=None,
        ),
        "LGBMRegressor-hi-10": Column(
            dtype="float64",
            checks=[
                Check.greater_than_or_equal_to(LOWER_BOUND),
                Check.less_than_or_equal_to(UPPER_BOUND),
            ],
            nullable=True,
            unique=False,
            coerce=False,
            required=True,
            regex=False,
            description=None,
            title=None,
        ),
        "LGBMRegressor-hi-20": Column(
            dtype="float64",
            checks=[
                Check.greater_than_or_equal_to(LOWER_BOUND),
                Check.less_than_or_equal_to(UPPER_BOUND),
            ],
            nullable=True,
            unique=False,
            coerce=False,
            required=True,
            regex=False,
            description=None,
            title=None,
        ),
        "LGBMRegressor-hi-30": Column(
            dtype="float64",
            checks=[
                Check.greater_than_or_equal_to(LOWER_BOUND),
                Check.less_than_or_equal_to(UPPER_BOUND),
            ],
            nullable=True,
            unique=False,
            coerce=False,
            required=True,
            regex=False,
            description=None,
            title=None,
        ),
        "LGBMRegressor-hi-40": Column(
            dtype="float64",
            checks=[
                Check.greater_than_or_equal_to(LOWER_BOUND),
                Check.less_than_or_equal_to(UPPER_BOUND),
            ],
            nullable=True,
            unique=False,
            coerce=False,
            required=True,
            regex=False,
            description=None,
            title=None,
        ),
        "LGBMRegressor-hi-50": Column(
            dtype="float64",
            checks=[
                Check.greater_than_or_equal_to(LOWER_BOUND),
                Check.less_than_or_equal_to(UPPER_BOUND),
            ],
            nullable=True,
            unique=False,
            coerce=False,
            required=True,
            regex=False,
            description=None,
            title=None,
        ),
        "LGBMRegressor-hi-60": Column(
            dtype="float64",
            checks=[
                Check.greater_than_or_equal_to(LOWER_BOUND),
                Check.less_than_or_equal_to(UPPER_BOUND),
            ],
            nullable=True,
            unique=False,
            coerce=False,
            required=True,
            regex=False,
            description=None,
            title=None,
        ),
        "LGBMRegressor-hi-70": Column(
            dtype="float64",
            checks=[
                Check.greater_than_or_equal_to(LOWER_BOUND),
                Check.less_than_or_equal_to(UPPER_BOUND),
            ],
            nullable=True,
            unique=False,
            coerce=False,
            required=True,
            regex=False,
            description=None,
            title=None,
        ),
        "LGBMRegressor-hi-80": Column(
            dtype="float64",
            checks=[
                Check.greater_than_or_equal_to(LOWER_BOUND),
                Check.less_than_or_equal_to(UPPER_BOUND),
            ],
            nullable=True,
            unique=False,
            coerce=False,
            required=True,
            regex=False,
            description=None,
            title=None,
        ),
        "LGBMRegressor-hi-90": Column(
            dtype="float64",
            checks=[
                Check.greater_than_or_equal_to(LOWER_BOUND),
                Check.less_than_or_equal_to(UPPER_BOUND),
            ],
            nullable=True,
            unique=False,
            coerce=False,
            required=True,
            regex=False,
            description=None,
            title=None,
        ),
        "LGBMRegressor-hi-95": Column(
            dtype="float64",
            checks=[
                Check.greater_than_or_equal_to(LOWER_BOUND),
                Check.less_than_or_equal_to(UPPER_BOUND),
            ],
            nullable=True,
            unique=False,
            coerce=False,
            required=True,
            regex=False,
            description=None,
            title=None,
        ),
        "LGBMRegressor-hi-99": Column(
            dtype="float64",
            checks=[
                Check.greater_than_or_equal_to(LOWER_BOUND),
                Check.less_than_or_equal_to(UPPER_BOUND),
            ],
            nullable=True,
            unique=False,
            coerce=False,
            required=True,
            regex=False,
            description=None,
            title=None,
        ),
        "XGBRegressor-lo-99": Column(
            dtype="float64",
            checks=[
                Check.greater_than_or_equal_to(LOWER_BOUND),
                Check.less_than_or_equal_to(UPPER_BOUND),
            ],
            nullable=True,
            unique=False,
            coerce=False,
            required=True,
            regex=False,
            description=None,
            title=None,
        ),
        "XGBRegressor-lo-95": Column(
            dtype="float64",
            checks=[
                Check.greater_than_or_equal_to(LOWER_BOUND),
                Check.less_than_or_equal_to(UPPER_BOUND),
            ],
            nullable=True,
            unique=False,
            coerce=False,
            required=True,
            regex=False,
            description=None,
            title=None,
        ),
        "XGBRegressor-lo-90": Column(
            dtype="float64",
            checks=[
                Check.greater_than_or_equal_to(LOWER_BOUND),
                Check.less_than_or_equal_to(UPPER_BOUND),
            ],
            nullable=True,
            unique=False,
            coerce=False,
            required=True,
            regex=False,
            description=None,
            title=None,
        ),
        "XGBRegressor-lo-80": Column(
            dtype="float64",
            checks=[
                Check.greater_than_or_equal_to(LOWER_BOUND),
                Check.less_than_or_equal_to(UPPER_BOUND),
            ],
            nullable=True,
            unique=False,
            coerce=False,
            required=True,
            regex=False,
            description=None,
            title=None,
        ),
        "XGBRegressor-lo-70": Column(
            dtype="float64",
            checks=[
                Check.greater_than_or_equal_to(LOWER_BOUND),
                Check.less_than_or_equal_to(UPPER_BOUND),
            ],
            nullable=True,
            unique=False,
            coerce=False,
            required=True,
            regex=False,
            description=None,
            title=None,
        ),
        "XGBRegressor-lo-60": Column(
            dtype="float64",
            checks=[
                Check.greater_than_or_equal_to(LOWER_BOUND),
                Check.less_than_or_equal_to(UPPER_BOUND),
            ],
            nullable=True,
            unique=False,
            coerce=False,
            required=True,
            regex=False,
            description=None,
            title=None,
        ),
        "XGBRegressor-lo-50": Column(
            dtype="float64",
            checks=[
                Check.greater_than_or_equal_to(LOWER_BOUND),
                Check.less_than_or_equal_to(UPPER_BOUND),
            ],
            nullable=True,
            unique=False,
            coerce=False,
            required=True,
            regex=False,
            description=None,
            title=None,
        ),
        "XGBRegressor-lo-40": Column(
            dtype="float64",
            checks=[
                Check.greater_than_or_equal_to(LOWER_BOUND),
                Check.less_than_or_equal_to(UPPER_BOUND),
            ],
            nullable=True,
            unique=False,
            coerce=False,
            required=True,
            regex=False,
            description=None,
            title=None,
        ),
        "XGBRegressor-lo-30": Column(
            dtype="float64",
            checks=[
                Check.greater_than_or_equal_to(LOWER_BOUND),
                Check.less_than_or_equal_to(UPPER_BOUND),
            ],
            nullable=True,
            unique=False,
            coerce=False,
            required=True,
            regex=False,
            description=None,
            title=None,
        ),
        "XGBRegressor-lo-20": Column(
            dtype="float64",
            checks=[
                Check.greater_than_or_equal_to(LOWER_BOUND),
                Check.less_than_or_equal_to(UPPER_BOUND),
            ],
            nullable=True,
            unique=False,
            coerce=False,
            required=True,
            regex=False,
            description=None,
            title=None,
        ),
        "XGBRegressor-lo-10": Column(
            dtype="float64",
            checks=[
                Check.greater_than_or_equal_to(LOWER_BOUND),
                Check.less_than_or_equal_to(UPPER_BOUND),
            ],
            nullable=True,
            unique=False,
            coerce=False,
            required=True,
            regex=False,
            description=None,
            title=None,
        ),
        "XGBRegressor-hi-10": Column(
            dtype="float64",
            checks=[
                Check.greater_than_or_equal_to(LOWER_BOUND),
                Check.less_than_or_equal_to(UPPER_BOUND),
            ],
            nullable=True,
            unique=False,
            coerce=False,
            required=True,
            regex=False,
            description=None,
            title=None,
        ),
        "XGBRegressor-hi-20": Column(
            dtype="float64",
            checks=[
                Check.greater_than_or_equal_to(LOWER_BOUND),
                Check.less_than_or_equal_to(UPPER_BOUND),
            ],
            nullable=True,
            unique=False,
            coerce=False,
            required=True,
            regex=False,
            description=None,
            title=None,
        ),
        "XGBRegressor-hi-30": Column(
            dtype="float64",
            checks=[
                Check.greater_than_or_equal_to(LOWER_BOUND),
                Check.less_than_or_equal_to(UPPER_BOUND),
            ],
            nullable=True,
            unique=False,
            coerce=False,
            required=True,
            regex=False,
            description=None,
            title=None,
        ),
        "XGBRegressor-hi-40": Column(
            dtype="float64",
            checks=[
                Check.greater_than_or_equal_to(LOWER_BOUND),
                Check.less_than_or_equal_to(UPPER_BOUND),
            ],
            nullable=True,
            unique=False,
            coerce=False,
            required=True,
            regex=False,
            description=None,
            title=None,
        ),
        "XGBRegressor-hi-50": Column(
            dtype="float64",
            checks=[
                Check.greater_than_or_equal_to(LOWER_BOUND),
                Check.less_than_or_equal_to(UPPER_BOUND),
            ],
            nullable=True,
            unique=False,
            coerce=False,
            required=True,
            regex=False,
            description=None,
            title=None,
        ),
        "XGBRegressor-hi-60": Column(
            dtype="float64",
            checks=[
                Check.greater_than_or_equal_to(LOWER_BOUND),
                Check.less_than_or_equal_to(UPPER_BOUND),
            ],
            nullable=True,
            unique=False,
            coerce=False,
            required=True,
            regex=False,
            description=None,
            title=None,
        ),
        "XGBRegressor-hi-70": Column(
            dtype="float64",
            checks=[
                Check.greater_than_or_equal_to(LOWER_BOUND),
                Check.less_than_or_equal_to(UPPER_BOUND),
            ],
            nullable=True,
            unique=False,
            coerce=False,
            required=True,
            regex=False,
            description=None,
            title=None,
        ),
        "XGBRegressor-hi-80": Column(
            dtype="float64",
            checks=[
                Check.greater_than_or_equal_to(LOWER_BOUND),
                Check.less_than_or_equal_to(UPPER_BOUND),
            ],
            nullable=True,
            unique=False,
            coerce=False,
            required=True,
            regex=False,
            description=None,
            title=None,
        ),
        "XGBRegressor-hi-90": Column(
            dtype="float64",
            checks=[
                Check.greater_than_or_equal_to(LOWER_BOUND),
                Check.less_than_or_equal_to(UPPER_BOUND),
            ],
            nullable=True,
            unique=False,
            coerce=False,
            required=True,
            regex=False,
            description=None,
            title=None,
        ),
        "XGBRegressor-hi-95": Column(
            dtype="float64",
            checks=[
                Check.greater_than_or_equal_to(LOWER_BOUND),
                Check.less_than_or_equal_to(UPPER_BOUND),
            ],
            nullable=True,
            unique=False,
            coerce=False,
            required=True,
            regex=False,
            description=None,
            title=None,
        ),
        "XGBRegressor-hi-99": Column(
            dtype="float64",
            checks=[
                Check.greater_than_or_equal_to(LOWER_BOUND),
                Check.less_than_or_equal_to(UPPER_BOUND),
            ],
            nullable=True,
            unique=False,
            coerce=False,
            required=True,
            regex=False,
            description=None,
            title=None,
        ),
        "RandomForestRegressor-lo-99": Column(
            dtype="float64",
            checks=[
                Check.greater_than_or_equal_to(LOWER_BOUND),
                Check.less_than_or_equal_to(UPPER_BOUND),
            ],
            nullable=True,
            unique=False,
            coerce=False,
            required=True,
            regex=False,
            description=None,
            title=None,
        ),
        "RandomForestRegressor-lo-95": Column(
            dtype="float64",
            checks=[
                Check.greater_than_or_equal_to(LOWER_BOUND),
                Check.less_than_or_equal_to(UPPER_BOUND),
            ],
            nullable=True,
            unique=False,
            coerce=False,
            required=True,
            regex=False,
            description=None,
            title=None,
        ),
        "RandomForestRegressor-lo-90": Column(
            dtype="float64",
            checks=[
                Check.greater_than_or_equal_to(LOWER_BOUND),
                Check.less_than_or_equal_to(UPPER_BOUND),
            ],
            nullable=True,
            unique=False,
            coerce=False,
            required=True,
            regex=False,
            description=None,
            title=None,
        ),
        "RandomForestRegressor-lo-80": Column(
            dtype="float64",
            checks=[
                Check.greater_than_or_equal_to(LOWER_BOUND),
                Check.less_than_or_equal_to(UPPER_BOUND),
            ],
            nullable=True,
            unique=False,
            coerce=False,
            required=True,
            regex=False,
            description=None,
            title=None,
        ),
        "RandomForestRegressor-lo-70": Column(
            dtype="float64",
            checks=[
                Check.greater_than_or_equal_to(LOWER_BOUND),
                Check.less_than_or_equal_to(UPPER_BOUND),
            ],
            nullable=True,
            unique=False,
            coerce=False,
            required=True,
            regex=False,
            description=None,
            title=None,
        ),
        "RandomForestRegressor-lo-60": Column(
            dtype="float64",
            checks=[
                Check.greater_than_or_equal_to(LOWER_BOUND),
                Check.less_than_or_equal_to(UPPER_BOUND),
            ],
            nullable=True,
            unique=False,
            coerce=False,
            required=True,
            regex=False,
            description=None,
            title=None,
        ),
        "RandomForestRegressor-lo-50": Column(
            dtype="float64",
            checks=[
                Check.greater_than_or_equal_to(LOWER_BOUND),
                Check.less_than_or_equal_to(UPPER_BOUND),
            ],
            nullable=True,
            unique=False,
            coerce=False,
            required=True,
            regex=False,
            description=None,
            title=None,
        ),
        "RandomForestRegressor-lo-40": Column(
            dtype="float64",
            checks=[
                Check.greater_than_or_equal_to(LOWER_BOUND),
                Check.less_than_or_equal_to(UPPER_BOUND),
            ],
            nullable=True,
            unique=False,
            coerce=False,
            required=True,
            regex=False,
            description=None,
            title=None,
        ),
        "RandomForestRegressor-lo-30": Column(
            dtype="float64",
            checks=[
                Check.greater_than_or_equal_to(LOWER_BOUND),
                Check.less_than_or_equal_to(UPPER_BOUND),
            ],
            nullable=True,
            unique=False,
            coerce=False,
            required=True,
            regex=False,
            description=None,
            title=None,
        ),
        "RandomForestRegressor-lo-20": Column(
            dtype="float64",
            checks=[
                Check.greater_than_or_equal_to(LOWER_BOUND),
                Check.less_than_or_equal_to(UPPER_BOUND),
            ],
            nullable=True,
            unique=False,
            coerce=False,
            required=True,
            regex=False,
            description=None,
            title=None,
        ),
        "RandomForestRegressor-lo-10": Column(
            dtype="float64",
            checks=[
                Check.greater_than_or_equal_to(LOWER_BOUND),
                Check.less_than_or_equal_to(UPPER_BOUND),
            ],
            nullable=True,
            unique=False,
            coerce=False,
            required=True,
            regex=False,
            description=None,
            title=None,
        ),
        "RandomForestRegressor-hi-10": Column(
            dtype="float64",
            checks=[
                Check.greater_than_or_equal_to(LOWER_BOUND),
                Check.less_than_or_equal_to(UPPER_BOUND),
            ],
            nullable=True,
            unique=False,
            coerce=False,
            required=True,
            regex=False,
            description=None,
            title=None,
        ),
        "RandomForestRegressor-hi-20": Column(
            dtype="float64",
            checks=[
                Check.greater_than_or_equal_to(LOWER_BOUND),
                Check.less_than_or_equal_to(UPPER_BOUND),
            ],
            nullable=True,
            unique=False,
            coerce=False,
            required=True,
            regex=False,
            description=None,
            title=None,
        ),
        "RandomForestRegressor-hi-30": Column(
            dtype="float64",
            checks=[
                Check.greater_than_or_equal_to(LOWER_BOUND),
                Check.less_than_or_equal_to(UPPER_BOUND),
            ],
            nullable=True,
            unique=False,
            coerce=False,
            required=True,
            regex=False,
            description=None,
            title=None,
        ),
        "RandomForestRegressor-hi-40": Column(
            dtype="float64",
            checks=[
                Check.greater_than_or_equal_to(LOWER_BOUND),
                Check.less_than_or_equal_to(UPPER_BOUND),
            ],
            nullable=True,
            unique=False,
            coerce=False,
            required=True,
            regex=False,
            description=None,
            title=None,
        ),
        "RandomForestRegressor-hi-50": Column(
            dtype="float64",
            checks=[
                Check.greater_than_or_equal_to(LOWER_BOUND),
                Check.less_than_or_equal_to(UPPER_BOUND),
            ],
            nullable=True,
            unique=False,
            coerce=False,
            required=True,
            regex=False,
            description=None,
            title=None,
        ),
        "RandomForestRegressor-hi-60": Column(
            dtype="float64",
            checks=[
                Check.greater_than_or_equal_to(LOWER_BOUND),
                Check.less_than_or_equal_to(UPPER_BOUND),
            ],
            nullable=True,
            unique=False,
            coerce=False,
            required=True,
            regex=False,
            description=None,
            title=None,
        ),
        "RandomForestRegressor-hi-70": Column(
            dtype="float64",
            checks=[
                Check.greater_than_or_equal_to(LOWER_BOUND),
                Check.less_than_or_equal_to(UPPER_BOUND),
            ],
            nullable=True,
            unique=False,
            coerce=False,
            required=True,
            regex=False,
            description=None,
            title=None,
        ),
        "RandomForestRegressor-hi-80": Column(
            dtype="float64",
            checks=[
                Check.greater_than_or_equal_to(LOWER_BOUND),
                Check.less_than_or_equal_to(UPPER_BOUND),
            ],
            nullable=True,
            unique=False,
            coerce=False,
            required=True,
            regex=False,
            description=None,
            title=None,
        ),
        "RandomForestRegressor-hi-90": Column(
            dtype="float64",
            checks=[
                Check.greater_than_or_equal_to(LOWER_BOUND),
                Check.less_than_or_equal_to(UPPER_BOUND),
            ],
            nullable=True,
            unique=False,
            coerce=False,
            required=True,
            regex=False,
            description=None,
            title=None,
        ),
        "RandomForestRegressor-hi-95": Column(
            dtype="float64",
            checks=[
                Check.greater_than_or_equal_to(LOWER_BOUND),
                Check.less_than_or_equal_to(UPPER_BOUND),
            ],
            nullable=True,
            unique=False,
            coerce=False,
            required=True,
            regex=False,
            description=None,
            title=None,
        ),
        "RandomForestRegressor-hi-99": Column(
            dtype="float64",
            checks=[
                Check.greater_than_or_equal_to(LOWER_BOUND),
                Check.less_than_or_equal_to(UPPER_BOUND),
            ],
            nullable=True,
            unique=False,
            coerce=False,
            required=True,
            regex=False,
            description=None,
            title=None,
        ),
    },
    checks=None,
    index=Index(
        dtype="int64",
        checks=[
            Check.greater_than_or_equal_to(0),
        ],
        nullable=False,
        coerce=False,
        name=None,
        description=None,
        title=None,
    ),
    dtype=None,
    coerce=True,
    strict=True,
    name=None,
    ordered=False,
    unique=None,
    report_duplicates="all",
    unique_column_names=False,
    add_missing_columns=False,
    title=None,
    description=None,
)
